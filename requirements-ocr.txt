# Optional local OCR enhancer for Agent 5 house-number validation.
# PaddlePaddle wheels are not available for every Python release on Windows.
# Python 3.11/3.12 is recommended if PaddleOCR must run locally.
# PaddleOCR 2.7.x requires NumPy 1.x (NumPy 2.0 removed np.sctypes).
numpy>=1.26.0,<2.0.0
opencv-python-headless>=4.8.0,<4.11.0
paddleocr>=2.7.0; python_version < "3.13"
paddlepaddle>=2.6.0; python_version < "3.13"
pytesseract>=0.3.13
