pandas>=2.2.0
openpyxl>=3.1.2
lxml>=5.1.0
fastkml>=1.0.0
shapely>=2.0.0
geopandas>=1.0.0
SQLAlchemy>=2.0.25
psycopg2-binary>=2.9.9
GeoAlchemy2>=0.14.3
python-dotenv>=1.0.1
pydantic>=2.6.0
pydantic-settings>=2.1.0
click>=8.1.7
pytest>=8.0.0
ruff>=0.3.0
requests>=2.31.0
loguru>=0.7.2
Pillow>=10.0.0
certifi>=2024.2.2
rapidfuzz>=3.6.0
tqdm>=4.66.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
# ── Redis / async task queue ──────────────────────────────
redis>=5.0
celery[redis]>=5.4
# ── JWT authentication ────────────────────────────────────
python-jose[cryptography]>=3.3
# ── Celery monitoring UI ──────────────────────────────────
flower>=2.0
# ── WebSocket support (native in FastAPI but explicit dep) ─
websockets>=12.0
# ── RabbitMQ — A2A agent bus (pika = official Python AMQP client) ────────────
pika>=1.3.2
# ── Test utilities ───────────────────────────────────────────────────────────
fakeredis>=2.21.0
